# This workflow has been superseded by ci.yml which runs tests, lint, and smoke
# before publishing. The publish job now lives in ci.yml as the final gated step.
# This file is retained for reference only and has no triggers.
name: publish image (retired)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason (this workflow is retired — use ci.yml)"
        required: false

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is retired. Publishing is handled by ci.yml."
